# Python
.venv/
__pycache__/
*.pyc

# Ephemeral per-session candle cache (wiped every launch)
*.db
tv_charts.db

# Chart snapshots are local reference artifacts; keep the folder, not the PNGs
img/*.png

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
